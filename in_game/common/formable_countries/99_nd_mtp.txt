MTP_f = {

	level = 3
	required_locations_fraction = 0.6
	rule = historical

	potential = {
		OR = {
			culture = culture:shona_culture
			culture = culture:kalanga_culture
		}
	}

	allow = {
		owns = location:great_zimbabwe
	}

	name = MTP
	flag = MTP
	adjective = MTP_ADJ
	tag = MTP
	color = map_MTP

	regions = {
		zimbabwe_region
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		trigger_event_non_silently = nd_mtp.1
		add_stability = stability_mild_bonus
	}
}
