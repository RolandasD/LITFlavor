REPLACE:MCH_f = {
	level = 3
	required_locations_fraction = 0.5
	rule = historical
	
	potential = {
		culture ?= { has_culture_group = culture_group:jurchen_group }
	}

	name = MCH
	flag = MCH
	adjective = MCH_ADJ
	tag = MCH
	color = map_MCH
	areas = {
		liaodong_area
		lower_amur_area
		middle_amur_area
		songhua_area
		songnen_area
		ussuri_area
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}
		trigger_event_non_silently = flavor_mch.5
		trigger_event_non_silently = nd_mch.1
		add_stability = stability_mild_bonus
	}
}
