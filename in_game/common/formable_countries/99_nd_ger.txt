REPLACE:GER_f = {
	level = 4
	required_locations_fraction = 0.66
	rule = plausible
	potential = {
		culture = { has_culture_group = culture_group:german_group }
	}

	name = GER
	flag = GER
	adjective = GER_ADJ
	tag = GER
	color = map_GER

	regions = {
		north_german_region
		south_german_region
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
		trigger_event_non_silently = nd_ger.1
		add_stability = stability_severe_bonus
	}
}
