# Landwehr — German citizen militia (specialized, must come first)
levy_nd_ger_landwehr = {
	size = levy_generic_infantry_size
	unit = a_nd_ger_landwehr

	allowed_pop_type = peasants
	allowed_pop_type = burghers

	country_allow = {
		has_or_had_tag = GER
	}
}
