##############################
# Moldavia (MOL) — Custom Levies
##############################

# Plaiesi — mountain border guards (most specialized, first)
levy_nd_mol_plaiesi = {
	size = levy_generic_infantry_size
	unit = a_nd_mol_plaiesi

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = MOL
	}
}

# Calarasi — light cavalry raiders
levy_nd_mol_calarasi = {
	size = levy_generic_noble_cavalry_size
	unit = a_nd_mol_calarasi

	allowed_pop_type = nobles

	country_allow = {
		has_or_had_tag = MOL
	}
}

# Razesi — free peasant militia
levy_nd_mol_razesi = {
	size = levy_generic_infantry_size
	unit = a_nd_mol_razesi

	allowed_pop_type = peasants

	country_allow = {
		has_or_had_tag = MOL
	}
}
