# Modern infantry levy (age 6) - must be first (most specialized)
levy_nd_ate_modern_infantry = {
	size = levy_generic_infantry_size
	unit = a_nd_ate_modern_infantry

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = ATE
	}
}

# Professional infantry levy (age 5)
levy_nd_ate_professional_infantry = {
	size = levy_generic_infantry_size
	unit = a_nd_ate_professional_infantry

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = ATE
	}
}

# Asafo musketeer levy (age 2)
levy_nd_ate_asafo_musketeer = {
	size = levy_generic_infantry_size
	unit = a_nd_ate_asafo_musketeer

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = ATE
	}
}

# Forest skirmisher levy (age 1)
levy_nd_ate_forest_skirmishers = {
	size = levy_generic_infantry_size
	unit = a_nd_ate_forest_skirmishers

	allowed_pop_type = peasants
	allowed_pop_type = soldiers

	country_allow = {
		has_or_had_tag = ATE
	}
}
